import React from 'react';

const Loading = () => {
    return (
        <div className='text-8xl font-bold flex items-center justify-center h-screen'>Loading...</div>
    );
};

export default Loading;